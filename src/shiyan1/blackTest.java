package shiyan1;

import static org.junit.Assert.*;

import org.junit.Test;
/*
public class blackTest {

    test t=new test();
    test.graph g=new test.graph("D:/lab1test/lab1test.txt");

    @Test
    public void testcalcShortestPath() {
        
        assertEquals(g.calcShortestPath("the", "one"),"thebigdatatimeservitizationbecomesone");//有最短路径的
       
        assertEquals(g.calcShortestPath("word", ""),null);//只输入一个单词的
        assertEquals(g.calcShortestPath("word", "in"),null);//没有路径的
        assertEquals(g.calcShortestPath("啊", "啊"),null);//非法字符
        assertEquals(g.calcShortestPath("", ""),null);//输入0个单词

    }


}
*/